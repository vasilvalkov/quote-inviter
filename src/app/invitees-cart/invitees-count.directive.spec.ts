import { InviteesCountDirective } from './invitees-count.directive';

describe('InviteesCountDirective', () => {
  it('should create an instance', () => {
    const directive = new InviteesCountDirective();
    expect(directive).toBeTruthy();
  });
});
