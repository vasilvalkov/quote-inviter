import { SelectedContactPipe } from './selected-contact.pipe';

describe('SelectedContactPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectedContactPipe();
    expect(pipe).toBeTruthy();
  });
});
